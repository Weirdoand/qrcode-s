{
  "permissions": {
    "allow": [
      "Bash(npm install *)",
      "Bash(npx --no-install tsc --noEmit)",
      "Bash(npx --no-install wrangler --version)",
      "Bash(wrangler --version)"
    ]
  }
}
